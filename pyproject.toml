[project]
name = "risk-rl"
version = "0.0.2"
readme = "README.md"
requires-python = ">=3.13,<3.14"

dependencies = [
    "pettingzoo~=1.26.0",
    "numpy~=2.2.6",
    "torch~=2.12.0",
    "torch_geometric~=2.8.0",
    "tianshou[eval]",
]

[project.optional-dependencies]
rocm = [
    "torch==2.12.0+rocm7.2 ; python_version == '3.13' and sys_platform == 'linux'",
    "triton-rocm==3.7.0 ; python_version == '3.13' and sys_platform == 'linux'",
]

[tool.uv.sources]
torch = { index = "rocm-index" }
triton-rocm = { index = "rocm-index" }
tianshou = { git = "https://github.com/thu-ml/tianshou.git", branch = "master" }

[[tool.uv.index]]
name = "rocm-index"
url = "https://download.pytorch.org/whl/rocm7.2"
explicit = true

[tool.uv]
environments = ["sys_platform == 'linux'"]

[tool.setuptools]
packages = ["agents", "environment"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"